import useSWR from 'swr';
// const fetcher = (...args) => fetch().then(res => res.json())
// function useUser (id) {
//     const { data, error } = useSWR(`/api/user/${id}`, fetcher)
  
//     return {
//       user: data,
//       isLoading: !error && !data,
//       isError: error
//     }
//   }

